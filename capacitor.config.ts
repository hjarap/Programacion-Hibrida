import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'evaluacion.v1',
  appName: 'evaluacion-1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
